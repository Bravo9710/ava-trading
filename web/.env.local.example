# Base URL of the headless WordPress site (no trailing slash, server-side only — no NEXT_PUBLIC_).
# The app fetches ${WORDPRESS_API_URL}/wp-json/wp/v2/avatrade_testimonial
WORDPRESS_API_URL=http://ava-trading-testimonials.local
